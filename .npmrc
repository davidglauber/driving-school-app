# Resolve peer dependency conflict: react-native-animated-nav-tab-bar
# declares react-native-screens@^3.x but the project uses ~4.4.0 (required by React Navigation).
# Legacy peer deps allow install; the tab bar typically works with screens v4.
legacy-peer-deps=true
