import { height, width } from "@/src/utils/dimensions";
import { StyleSheet } from "react-native";

export const logo_header_styles = StyleSheet.create({
    image: {
        width: width * 0.3, height: height * 0.1 
    },
})